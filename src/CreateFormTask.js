import React from 'react';

const CreateFormTask = (props) => {
    return (
        <div>
            CreateFormTask
            <input/>
            <button>create new task</button>

        </div>
    )

}

export default CreateFormTask
